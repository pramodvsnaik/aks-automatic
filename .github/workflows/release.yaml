name: deploy-aks-automatic
on:
 workflow_dispatch:
   
jobs:
  deploy-aks-automatic:
    runs-on: ubuntu-latest
    steps:
        - name: checkout
          uses: actions/checkout@v6.0.2
          
        - name: deploy-aks
          run: |
           echo "Set Environment Variables"
           ../automatic/custom-network/private/sh/define-vars.sh
           echo ${RG_NAME}
           echo ${VNET_NAME}        
    
